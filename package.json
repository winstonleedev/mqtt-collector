{
  "name": "mqtt-collector",
  "version": "1.0.0",
  "description": "data collector from AMQP to MongoDB",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.1",
    "dotenv": "^4.0.0",
    "mongoose": "^4.11.12",
    "promise": "^8.0.1"
  }
}
